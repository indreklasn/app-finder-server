type Product {
  id: ID!
  name: String!
  url: String!
  tags: [String]
}

type Query {
  products: [Product]
  getProductById(id: ID!): [Product]
}

type Mutation {
  addProduct(name: String!, url: String!, tags: [String]): [Product]
}
